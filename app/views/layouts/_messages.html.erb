<% flash.each do |message_type, message_contant| %>
<div class="ui <%= message_type%> message transition">
    <i class="close icon"></i>
    <div class="header">
        <%= message_contant %>
    </div>
</div>

<%end%>