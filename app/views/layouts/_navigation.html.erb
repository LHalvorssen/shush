<%= link_to image_tag("/logo.png"), root_path, class: "item" %>
<% if logged_in? %>
  <%= link_to "Chatroom", chatroom_path, class: "item" %>
<%end%>


<div class="right menu">
  <% if logged_in? %>
    <div class="ui dropdown item">
    important stuff <i class="dropdown icon"></i>
      <div class="menu">
        <%= link_to "Leave this place", logout_path, method: :delete, class: "item" %>
      </div>
    </div>
  <%end%>

  <% if !logged_in? %>
  <div class="item">
    <%= link_to "Get in", login_path, class: "ui primary button" %>
  </div>
  <%end%>
</div>